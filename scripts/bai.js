      // console.log(chunk.length);
      // console.log(` magic: ${chunk.readUInt8(0).toString(16)}`);
      // console.log(` magic: ${chunk.readUInt8(1).toString(16)}`);
      // console.log(` magic: ${chunk.readUInt8(2).toString(16)}`);
      // console.log(` magic: ${chunk.readUInt8(3).toString(16)}`);
      // console.log(` n_ref: ${chunk.readInt32LE(4)}`);
      // console.log(`   n_bin: ${chunk.readInt32LE(8)}`);
      // console.log(`     bin : ${chunk.readUInt32LE(12)}`);
      // console.log(`     nchnk: ${chunk.readInt32LE(16)}`);
      // //chunk start is a 64 bit
      // console.log(`       chnks: ${chunk.readUInt32LE(20)}`);
      // console.log(`       chnks: ${chunk.readUInt32LE(24)}`);
      // //chunk end is a 64 bit
      // console.log(`       chnke: ${chunk.readUInt32LE(28)}`);
      // console.log(`       chnke: ${chunk.readUInt32LE(32)}`);
      //
      //
      // console.log(`     bin : ${chunk.readUInt32LE(36)}`);
      // console.log(`     nchnk: ${chunk.readInt32LE(40)}`);
      // //chunk start is a 64 bit
      // console.log(`       chnks: ${chunk.readUInt32LE(44)}`);
      // console.log(`       chnks: ${chunk.readUInt32LE(48)}`);
      // //chunk end is a 64 bit
      // console.log(`       chnke: ${chunk.readUInt32LE(52)}`);
      // console.log(`       chnke: ${chunk.readUInt32LE(56)}`);
      // //chunk start is a 64 bit
      // console.log(`       n_map: ${chunk.readUInt32LE(60)}`);
      // console.log(`       n_ump: ${chunk.readUInt32LE(64)}`);
      //
      // console.log(` n_int: ${chunk.readUInt32LE(68)}`);
